<li>{{city.name}}
  <button {{action 'delete' city}}>Delete this city</button>

  <ul>
    {{#each city.attractions as |attraction|}}
      <li>{{attraction}}</li>
    {{/each}}
  </ul>

</li>
{{yield}}
